<UserControl x:Class="ClienteApiWebNetCore.Modules.ModuleName.Views.MenuSuperiorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ClienteApiWebNetCore.Modules.ModuleName.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" 
             Width="auto" Height="80">
    <Grid Background="SteelBlue">
        <StackPanel Orientation="Horizontal" >
            <Button x:Name="btnNuevoCertificado" Width="auto" Height="40" FontSize="20" VerticalAlignment="Center"
                    Margin="5,1,1,1"
                    HorizontalAlignment="Center"
                    Command="{Binding NuevoCertificadoCommand}" 
                    Content="Nuevo Certificado" 
                    IsDefault="True">
            </Button>

            <Button x:Name="btnAbrirCertificado" Width="auto" Height="40" FontSize="20" VerticalAlignment="Center"
                    Margin="5,1,1,1"
                    HorizontalAlignment="Center"
                    Command="{Binding AbrirCertificadoCommand}" 
                    Content="Abrir Certificado" 
                    IsDefault="True">
            </Button>

        </StackPanel>

        
    </Grid>
</UserControl>
